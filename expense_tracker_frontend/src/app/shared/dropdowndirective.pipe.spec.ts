import { DropdowndirectivePipe } from './dropdowndirective.pipe';

describe('DropdowndirectivePipe', () => {
  it('create an instance', () => {
    const pipe = new DropdowndirectivePipe();
    expect(pipe).toBeTruthy();
  });
});
